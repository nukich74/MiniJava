#include "BlockTracing.h"

namespace Canon {

	using namespace IRTree;

	//void Tracer::Rebuild( const CStmtList& list ) { 
	//	while( true ) {
	//		const IStmt* curStmt = list.GetCurrent();
	//		if( curStmt == 0 ) {
	//			break;
	//		} else {
	//			
	//		}
	//	}
	//}

}